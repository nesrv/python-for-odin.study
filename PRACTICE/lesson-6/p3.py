# информация о посещениях учеников разных кружков
d =  {
    "Алексей": {"программирование": 5, "математика": 3},
    "Мария": {"физика": 4, "математика": 6},
    "Иван": {"программирование": 2, "физика": 3},
    "Елена": {"математика": 5}
}

# словарь словарей
# список словарей
# кортеж словарей
# множество словарей?

# Пусть "Алексей" посетил "физику" 1 раз.

d["Алексей"]["физика"] = 1
d["Алексей"]["физика"] = d["Алексей"].get("физика", 0) + 2

# print(d['Алексей'])

# посчитать сумму посещений для каждого ученика 
# найти самого активно-посещаемого ученика

# Переменная для хранения максимального количества посещений
max_visit = 0

# Проходим по каждому пользователю и его данным в словаре d
for user, objects in d.items():
    # Суммируем все значения посещений для текущего пользователя
    s = sum(objects.values())     
    # Если текущая сумма больше максимальной
    if s > max_visit:
        name = user        # Запоминаем имя самого активного пользователя
        max_visit = s      # Обновляем максимальное количество посещений
    
# Выводим результат

# print(f"Самый активный - {name} посетил {max_visit} занятий")

# Статистика по кружку "математика"
# Считаем количество учеников и среднее:

# количество учеников по математике
# Счетчик студентов, изучающих математику

# Счетчик студентов, изучающих математику
math_students = 0
# Общее количество часов по математике у всех студентов
math_visits = 0

# Проходим по каждому студенту и его предметам
for user, objects in d.items():
    # Проверяем, изучает ли студент математику
    if 'математика' in objects:
        print(objects)  # Выводим данные студента для проверки
        math_students += 1  # Увеличиваем счетчик студентов
        math_visits += objects['математика']  # Добавляем часы математики этого студента

# Вычисляем и выводим среднее количество часов математики на одного студента
print(math_visits / math_students)



new_data = {"Ольга": {"физика": 2}, "Иван": {"физика": 1}}
# обновить данные в словаре новым словарем new_data
# задание на дом

